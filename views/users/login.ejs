<%- include("../partials/page_header", {title: "Login", subtitle: "Login to Model Deployer"}) %>

<p>Login to Model Deployer with your account details below.</p>
<p>If you don't have an account <a href="/users/signup">Sign Up</a>.</p>

<form method="POST" hx-post="<%= action %>" hx-target="#container" hx-swap="innerHTML" action="<%= action %>" class="flex flex-col w-full space-y-4">
    <label>
        <div>Email</div>
        <input type="email" name="email" value="<%= user.email %>" class="w-full" />
    </label>

    <label>
        <div>Password</div>
        <input type="password" name="password" value="" class="w-full" />
    </label>

    <button type="submit">Login</button>
</form>

<%- include("../partials/page_footer") %>